/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['pixabay.com','cdn.pixabay.com','images.unsplash.com','via.placeholder.com'],
  },
}

module.exports = nextConfig;